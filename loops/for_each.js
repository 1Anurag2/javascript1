const array = [1,2,3,4,5,6];

array.forEach(element => {
    console.log(element)
})
// const newArray = array.forEach(element => {
//     console.log(element)
// });

// forEach() is used only for iteration — it does not return a new array.
// So newArray will be undefined because forEach() always returns undefined.

// console.log(newArray)